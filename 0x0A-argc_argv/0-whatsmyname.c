#include <stdio.h>
#include <stdlib.h>
/**
 * main- a program that prints its name
 *
 * @argc: holds number of arguments to be passed
 * @argv: one dimensional array
 *
 * Return: Always (0)
 */

int main(int argc, char *argv[])
{
	int i = 0;

	(void) argc;
	printf("%s\n", argv[i]);
	return (0);
}
